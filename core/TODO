- documentation update

- (daemon) add gpm client code

- monitor /dev/tty0 for keypresses (?)

- convert all printerr/printwarn to iprint

- handle con2fb_map properly

- clean up the code

- fix handling of redrawing the inter boxes

- animations handling:
  - move code to correct places
  - add more comments
  - handle 'proportional' and 'done'

- add effects (fadein/fadeout/blendin) for icon/anim

- add a dirty tiles/rects system to handle prep_bgnds
  in a more sensible way

- make the parser more user-friendly

- fix error messages (doubled messages, etc)
  - use log_warn and log_err

New features:
=============

- separate progress bars for shutdown/bootup

- automatic theme resizing
